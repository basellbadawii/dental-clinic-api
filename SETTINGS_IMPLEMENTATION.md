# تنفيذ صفحة الإعدادات - Settings Page Implementation

## ✅ ما تم إنجازه

### 1. إنشاء جدول clinic_settings في Supabase
- **الملف**: `supabase_setup.sql`
- **الجدول الجديد**: `clinic_settings`
- **الحقول المضافة**:
  - معلومات العيادة: `clinic_name`, `clinic_phone`, `clinic_email`, `clinic_address`
  - إعدادات WhatsApp: `whatsapp_enabled`, `whatsapp_phone`, `whatsapp_api_key`
  - إعدادات n8n: `n8n_enabled`, `n8n_webhook_url`, `n8n_api_key`
  - مواعيد العمل: `working_hours` (JSONB)

### 2. إنشاء خدمة التعامل مع البيانات
- **الملف**: `src/services/settingsService.js`
- **الوظائف المتوفرة**:
  - `getClinicSettings()` - جلب الإعدادات
  - `updateClinicInfo()` - تحديث معلومات العيادة
  - `updateWhatsAppSettings()` - تحديث إعدادات WhatsApp
  - `updateN8nSettings()` - تحديث إعدادات n8n
  - `updateWorkingHours()` - تحديث مواعيد العمل

### 3. إنشاء صفحة الإعدادات
- **الملف**: `src/pages/Settings.jsx`
- **الأقسام المضافة**:
  1. **معلومات العيادة** - اسم العيادة، الهاتف، البريد الإلكتروني، العنوان
  2. **إعدادات ربط الواتساب** - تفعيل/تعطيل، رقم الواتساب، API Key
  3. **إعدادات ربط n8n** - تفعيل/تعطيل، Webhook URL، API Key
  4. **مواعيد العمل** - تحديد أوقات العمل لكل يوم من أيام الأسبوع

### 4. ربط الصفحة بالتطبيق
- **الملفات المحدثة**:
  - `src/App.jsx` - إضافة route `/settings`
  - `src/components/layout/Sidebar.jsx` - تحويل زر الإعدادات إلى NavLink يعمل

## 🎨 المميزات

- ✅ واجهة RTL كاملة باللغة العربية
- ✅ تصميم طبي مريح ومتناسق مع باقي التطبيق
- ✅ حفظ منفصل لكل قسم
- ✅ رسائل نجاح/خطأ واضحة
- ✅ تفعيل/تعطيل الإعدادات بشكل ديناميكي
- ✅ مواعيد عمل مرنة لكل يوم من أيام الأسبوع

## 📋 خطوات التشغيل

### 1. تحديث قاعدة البيانات
قم بتشغيل السكريبت التالي في Supabase SQL Editor:
```bash
# افتح ملف supabase_setup.sql ونفذ السكريبت بالكامل في Supabase
```

### 2. التأكد من البيئة
تأكد من وجود ملف `.env` بالإعدادات الصحيحة:
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key
```

### 3. تشغيل التطبيق
```bash
npm run dev
```

### 4. الوصول لصفحة الإعدادات
- انقر على زر "الإعدادات" في القائمة الجانبية
- أو اذهب مباشرة إلى: `http://localhost:5173/settings`

## 🔧 الاستخدام

### حفظ معلومات العيادة
1. أدخل اسم العيادة ورقم الهاتف والبريد الإلكتروني والعنوان
2. انقر على "حفظ المعلومات"

### تفعيل WhatsApp
1. فعّل خيار "تفعيل إرسال رسائل الواتساب"
2. أدخل رقم الواتساب بصيغة دولية (مثال: 966XXXXXXXXX)
3. أدخل API Key الخاص بـ Evolution API
4. انقر على "حفظ إعدادات الواتساب"

### تفعيل n8n
1. فعّل خيار "تفعيل تكامل n8n"
2. أدخل Webhook URL الخاص بـ n8n
3. أدخل API Key (اختياري)
4. انقر على "حفظ إعدادات n8n"

### تحديد مواعيد العمل
1. حدد الأيام التي تعمل فيها العيادة
2. حدد وقت البداية والنهاية لكل يوم
3. انقر على "حفظ مواعيد العمل"

## 📊 هيكل البيانات

### جدول clinic_settings
```sql
{
  id: UUID,
  clinic_name: VARCHAR(255),
  clinic_phone: VARCHAR(50),
  clinic_email: VARCHAR(255),
  clinic_address: TEXT,
  whatsapp_enabled: BOOLEAN,
  whatsapp_phone: VARCHAR(50),
  whatsapp_api_key: TEXT,
  n8n_enabled: BOOLEAN,
  n8n_webhook_url: TEXT,
  n8n_api_key: TEXT,
  working_hours: JSONB,
  created_at: TIMESTAMPTZ,
  updated_at: TIMESTAMPTZ
}
```

### مثال على working_hours
```json
{
  "saturday": {"enabled": true, "start": "09:00", "end": "17:00"},
  "sunday": {"enabled": true, "start": "09:00", "end": "17:00"},
  "monday": {"enabled": true, "start": "09:00", "end": "17:00"},
  "tuesday": {"enabled": true, "start": "09:00", "end": "17:00"},
  "wednesday": {"enabled": true, "start": "09:00", "end": "17:00"},
  "thursday": {"enabled": true, "start": "09:00", "end": "17:00"},
  "friday": {"enabled": false, "start": "09:00", "end": "17:00"}
}
```

## 🎯 التطويرات المستقبلية المقترحة

- [ ] رفع شعار العيادة
- [ ] إعدادات الألوان الخاصة بالتطبيق
- [ ] إعدادات الإشعارات والتنبيهات
- [ ] إعدادات الطباعة والتقارير
- [ ] نسخ احتياطي واستعادة البيانات
- [ ] إدارة المستخدمين والصلاحيات

## ✅ الخلاصة

تم تنفيذ صفحة الإعدادات بنجاح مع:
- ✅ إصلاح زر الإعدادات في القائمة الجانبية
- ✅ صفحة إعدادات كاملة بـ 4 أقسام رئيسية
- ✅ جدول clinic_settings في قاعدة البيانات
- ✅ واجهة RTL عربية مريحة
- ✅ حفظ منفصل لكل قسم

الآن يمكن للمستخدم الوصول لصفحة الإعدادات وتخصيص معلومات العيادة وإعدادات التكاملات ومواعيد العمل بسهولة! 🎉
