# دليل حل المشاكل - Troubleshooting Guide

## 🔧 مشكلة: الصفحة البيضاء عند فتح التطبيق

### الأسباب المحتملة والحلول:

---

### ✅ 1. مشكلة في التوجيه (Routing)

**السبب:** التطبيق يحتاج للدخول من صفحة `/login` أولاً

**الحل:**
```
افتح: http://localhost:5173/login
```

التطبيق يستخدم نظام مصادقة (Authentication)، وإذا لم تكن مسجل دخول، سيتم توجيهك تلقائياً لصفحة تسجيل الدخول.

**بيانات الدخول الافتراضية:**
- البريد الإلكتروني: `doctor@clinic.com`
- كلمة المرور: `password123`

---

### ✅ 2. فحص Console للأخطاء

**الخطوات:**
1. افتح التطبيق في المتصفح
2. اضغط `F12` لفتح Developer Tools
3. اذهب لتبويب `Console`
4. ابحث عن رسائل خطأ باللون الأحمر

**الأخطاء الشائعة:**
- `Module not found` - مكتبة ناقصة
- `Unexpected token` - خطأ في الـ Syntax
- `Cannot read property` - خطأ في البيانات

---

### ✅ 3. مسح الـ Cache

إذا كانت المشكلة مستمرة، امسح الـ cache:

```bash
# في PowerShell
cd dental-clinic-app
Remove-Item -Path dist -Recurse -Force
Remove-Item -Path node_modules/.vite -Recurse -Force
npm run dev
```

---

### ✅ 4. التحقق من المكتبات المطلوبة

**المكتبات الأساسية:**
```json
{
  "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "react-router-dom": "^6.22.0",
  "zustand": "^4.5.0",
  "lucide-react": "^0.344.0",
  "@supabase/supabase-js": "^2.39.7",
  "react-hot-toast": "^2.4.1"
}
```

**تثبيت المكتبات:**
```bash
npm install
```

---

### ✅ 5. فحص ملف .env

تأكد من وجود ملف `.env` في المجلد الرئيسي:

```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**ملاحظة:** إذا لم يكن لديك Supabase، التطبيق سيعمل ببيانات تجريبية.

---

### ✅ 6. إعادة البناء الكامل

في حالة استمرار المشكلة:

```bash
# 1. حذف المجلدات
Remove-Item -Path node_modules -Recurse -Force
Remove-Item -Path dist -Recurse -Force

# 2. إعادة التثبيت
npm install

# 3. البناء
npm run build

# 4. التشغيل
npm run dev
```

---

### ✅ 7. التحقق من Port 5173

إذا كان Port مشغول:

```bash
# تغيير Port في vite.config.js
server: {
  port: 3000  // أو أي port آخر
}
```

---

## 🐛 الأخطاء الشائعة وحلولها

### خطأ: "Cannot read property 'isAuthenticated'"

**الحل:**
```javascript
// تأكد من تهيئة authStore بشكل صحيح
const { isAuthenticated } = useAuthStore()
```

---

### خطأ: "Module not found: lucide-react"

**الحل:**
```bash
npm install lucide-react
```

---

### خطأ: "Hydration failed"

**الحل:**
```bash
# امسح الـ cache
rm -rf node_modules/.vite
npm run dev
```

---

## 📱 اختبار سريع

### 1. اختبار الخادم:
```bash
npm run dev
```
يجب أن ترى:
```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 2. اختبار البناء:
```bash
npm run build
```
يجب أن ينتهي بدون أخطاء وإنشاء مجلد `dist`

### 3. اختبار الصفحات:
- `http://localhost:5173/login` - صفحة تسجيل الدخول
- `http://localhost:5173/` - لوحة التحكم (بعد تسجيل الدخول)

---

## 🔍 الفحص المتقدم

### فحص الـ Network في Developer Tools:

1. افتح `F12` → تبويب `Network`
2. حدّث الصفحة
3. تحقق من:
   - `index.html` - يجب أن يُحمّل (200 OK)
   - `main.jsx` - يجب أن يُحمّل (200 OK)
   - ملفات CSS و JS - كلها يجب أن تُحمّل

---

## ✅ قائمة التحقق السريعة

- [ ] الخادم يعمل على `http://localhost:5173`
- [ ] ملف `.env` موجود ومُعد بشكل صحيح
- [ ] جميع المكتبات مثبتة (`npm install`)
- [ ] لا توجد أخطاء في Console
- [ ] جربت فتح `/login` مباشرة
- [ ] مسحت الـ cache وأعدت التشغيل

---

## 🆘 إذا استمرت المشكلة

1. **افحص ملف `src/main.jsx`:**
   ```javascript
   // يجب أن يكون:
   ReactDOM.createRoot(document.getElementById('root')).render(
     <React.StrictMode>
       <App />
     </React.StrictMode>,
   )
   ```

2. **افحص ملف `index.html`:**
   ```html
   <!-- يجب أن يحتوي على: -->
   <div id="root"></div>
   <script type="module" src="/src/main.jsx"></script>
   ```

3. **افحص ملف `App.jsx`:**
   - تأكد من وجود `<Router>` و `<Routes>`
   - تأكد من استيراد جميع الصفحات

---

## 📞 الحصول على المساعدة

إذا لم تُحل المشكلة:

1. التقط صورة شاشة للـ Console (F12)
2. أرسل رسالة الخطأ الكاملة
3. اذكر الخطوات التي جربتها

---

**آخر تحديث:** 10 فبراير 2026
