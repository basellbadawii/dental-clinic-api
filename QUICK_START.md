# 🚀 البدء السريع - نظام إدارة عيادة الأسنان

## ⚡ التشغيل السريع (5 دقائق)

### الخطوة 1️⃣: التثبيت

#### Windows:
```bash
cd dental-clinic-app
scripts\install.bat
```

#### Mac/Linux:
```bash
cd dental-clinic-app
chmod +x scripts/install.sh
./scripts/install.sh
```

### الخطوة 2️⃣: إعداد قاعدة البيانات

1. اذهب إلى [Supabase.com](https://supabase.com)
2. أنشئ مشروع جديد (مجاني)
3. اذهب إلى **SQL Editor**
4. انسخ الكود من `SETUP_GUIDE.md` (قسم إنشاء الجداول)
5. نفذ الكود

### الخطوة 3️⃣: إعداد المتغيرات

افتح ملف `.env` وأضف:

```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

احصل على هذه البيانات من Supabase → Settings → API

### الخطوة 4️⃣: التشغيل

#### Windows:
```bash
scripts\start.bat
```

#### Mac/Linux:
```bash
chmod +x scripts/start.sh
./scripts/start.sh
```

أو يدوياً:
```bash
npm run dev
```

### الخطوة 5️⃣: افتح المتصفح

اذهب إلى: **http://localhost:3000**

📧 **البريد:** admin@dental.com  
🔑 **كلمة المرور:** admin123

---

## 🎯 الاستخدام الأساسي

### إضافة مريض جديد
1. من القائمة الجانبية → **سجل المرضى**
2. اضغط **إضافة مريض جديد**
3. املأ البيانات واحفظ

### حجز موعد
1. اذهب إلى **جدول المواعيد**
2. اضغط **حجز موعد جديد**
3. اختر المريض والتاريخ والوقت
4. احفظ

### تسجيل زيارة
1. اذهب إلى **سجل الزيارات**
2. اضغط **تسجيل زيارة جديدة**
3. اختر المريض ونوع الخدمة
4. أدخل التكلفة واحفظ

---

## 💬 إعداد الواتساب (اختياري)

### باستخدام Docker:
```bash
docker run -d \
  --name evolution-api \
  -p 8080:8080 \
  -e AUTHENTICATION_API_KEY=my-secret-key \
  atendai/evolution-api:latest
```

### المسح والتفعيل:
1. افتح: http://localhost:8080
2. أنشئ instance جديد باسم `dental_clinic`
3. امسح QR Code من واتساب

### إضافة المتغيرات:
```env
VITE_EVOLUTION_API_URL=http://localhost:8080
VITE_EVOLUTION_API_KEY=my-secret-key
VITE_EVOLUTION_INSTANCE_NAME=dental_clinic
```

---

## 🔗 إعداد n8n (اختياري)

### التثبيت:
```bash
npm install -g n8n
n8n start
```

### الاستيراد:
1. افتح n8n على http://localhost:5678
2. اذهب إلى Workflows → Import
3. استورد `n8n-workflows/appointment-reminder.json`
4. فعّل الـ workflow

---

## 🐛 حل المشاكل الشائعة

### لا يعمل التطبيق؟
- تأكد من تثبيت Node.js (v18+)
- تأكد من تشغيل `npm install`
- تحقق من ملف `.env`

### خطأ في الاتصال بـ Supabase؟
- تحقق من صحة `SUPABASE_URL` و `SUPABASE_ANON_KEY`
- تأكد من إنشاء الجداول في قاعدة البيانات

### الواتساب لا يرسل؟
- تأكد من تشغيل Evolution API
- تحقق من مسح QR Code
- تأكد من صحة API Key

---

## 📚 ملفات مفيدة

- `SETUP_GUIDE.md` - دليل الإعداد الكامل
- `FEATURES.md` - قائمة المميزات
- `README.md` - نظرة عامة

---

## 🆘 المساعدة

واجهت مشكلة؟
- راجع `SETUP_GUIDE.md` للتفاصيل
- افتح Issue على GitHub
- راسلنا: support@dentalclinic.com

---

**جاهز للاستخدام! 🎉**
