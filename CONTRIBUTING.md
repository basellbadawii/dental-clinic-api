# 🤝 المساهمة في المشروع

شكراً لاهتمامك بالمساهمة في نظام إدارة عيادة الأسنان! نرحب بجميع المساهمات.

## 🌟 كيف يمكنك المساهمة؟

### 1. الإبلاغ عن الأخطاء (Bug Reports)
إذا وجدت خطأ:
- افتح Issue جديد
- صف المشكلة بالتفصيل
- أرفق صور أو فيديو إن أمكن
- ذكر نظام التشغيل والمتصفح

### 2. اقتراح ميزات جديدة (Feature Requests)
لديك فكرة رائعة؟
- افتح Issue مع وسم "enhancement"
- اشرح الميزة المقترحة
- وضح الفائدة المتوقعة
- أضف أمثلة إن أمكن

### 3. تحسين الكود (Pull Requests)
تريد إضافة ميزة أو إصلاح خطأ؟

#### الخطوات:
```bash
# 1. Fork المشروع
# 2. استنسخ Fork الخاص بك
git clone https://github.com/your-username/dental-clinic-app.git

# 3. أنشئ فرع جديد
git checkout -b feature/amazing-feature

# 4. قم بالتعديلات
# 5. Commit التغييرات
git commit -m "إضافة: ميزة رائعة جديدة"

# 6. Push إلى الفرع
git push origin feature/amazing-feature

# 7. افتح Pull Request
```

### 4. تحسين الوثائق
- إصلاح أخطاء إملائية
- إضافة أمثلة
- ترجمة إلى لغات أخرى
- تحسين الشروحات

## 📋 معايير الكود

### JavaScript/React
```javascript
// ✅ استخدم const/let بدلاً من var
const patientName = 'محمد'
let age = 30

// ✅ استخدم Arrow Functions
const handleClick = () => {
  console.log('clicked')
}

// ✅ استخدم Template Literals
const message = `مرحباً ${patientName}`

// ✅ استخدم Destructuring
const { name, age } = patient

// ✅ أضف PropTypes أو TypeScript
MyComponent.propTypes = {
  name: PropTypes.string.isRequired
}
```

### تنسيق الكود
```bash
# استخدم Prettier
npm run format

# تحقق من الأخطاء
npm run lint
```

### التسمية
- **Components**: PascalCase (`PatientCard.jsx`)
- **Functions**: camelCase (`handleSubmit`)
- **Constants**: UPPER_SNAKE_CASE (`API_URL`)
- **Files**: kebab-case للملفات العامة

### التعليقات
```javascript
// ✅ تعليقات واضحة بالعربية أو الإنجليزية
// جلب بيانات المرضى من قاعدة البيانات
const fetchPatients = async () => {
  // ...
}

// ❌ تجنب التعليقات الزائدة
// هذه دالة لجلب المرضى
const fetchPatients = async () => {} // دالة جلب
```

## 🧪 الاختبارات

قبل إرسال PR:
```bash
# تشغيل الاختبارات (إن وجدت)
npm test

# تشغيل التطبيق والتحقق من عمله
npm run dev

# بناء المشروع
npm run build
```

## 📝 رسائل Commit

استخدم رسائل واضحة:
```bash
# ✅ جيد
git commit -m "إصلاح: خطأ في حساب العمر"
git commit -m "إضافة: ميزة إرسال رسائل SMS"
git commit -m "تحسين: أداء جدول المرضى"

# ❌ سيء
git commit -m "تعديلات"
git commit -m "fix"
```

### أنواع Commits
- `إضافة:` / `feat:` - ميزة جديدة
- `إصلاح:` / `fix:` - إصلاح خطأ
- `تحسين:` / `improve:` - تحسين كود موجود
- `وثائق:` / `docs:` - تحديث الوثائق
- `تنسيق:` / `style:` - تغييرات تنسيق فقط
- `اختبار:` / `test:` - إضافة اختبارات

## 🎨 تصميم UI/UX

### الألوان
```javascript
// الألوان الرسمية للمشروع
primary: '#0ea5e9'      // أزرق فاتح
secondary: '#6b7280'    // رمادي
success: '#10b981'      // أخضر
danger: '#ef4444'       // أحمر
warning: '#f59e0b'      // برتقالي
```

### المسافات
استخدم نظام Tailwind:
- `p-4` = padding: 1rem
- `m-6` = margin: 1.5rem
- `gap-4` = gap: 1rem

## 🌍 الترجمة

نرحب بالترجمة إلى لغات أخرى:
```
src/
  locales/
    ar.json    # العربية (موجود)
    en.json    # الإنجليزية (مطلوب)
    fr.json    # الفرنسية (مطلوب)
```

## ⚖️ الترخيص

بمساهمتك، فإنك توافق على ترخيص مساهمتك تحت MIT License.

## 📞 التواصل

- **GitHub Issues**: للمشاكل التقنية
- **Discussions**: للنقاشات والأسئلة
- **Email**: dev@dentalclinic.com

## 🙏 شكر خاص

شكراً لكل من ساهم في تحسين المشروع:
- المطورين
- المختبرين
- مصممي UI/UX
- كاتبي الوثائق
- المترجمين

---

**كل مساهمة، مهما كانت صغيرة، تُحدث فرقاً! 💙**
