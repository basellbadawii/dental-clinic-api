# دليل إعداد Supabase - نظام إدارة عيادة الأسنان

## 📋 الخطوات المطلوبة

### ✅ 1. تم تحديث ملف `.env`
تم تحديث ملف `.env` ببيانات الربط الخاصة بـ Supabase:

```env
VITE_SUPABASE_URL=https://dnzuljmguutvummtwljj.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### ✅ 2. لم يتم العثور على مشكلة أيقونة Tooth
- تم فحص جميع ملفات المشروع
- لا يوجد استخدام لأيقونة `Tooth` في الكود
- الملف `Login.jsx` يستخدم أيقونة `Activity` بشكل صحيح
- إذا كانت هناك مشكلة `SyntaxError`، يرجى تزويدنا برسالة الخطأ الدقيقة

### 📊 3. إنشاء الجداول في Supabase

#### الطريقة الأولى: استخدام SQL Editor (موصى بها)

1. **افتح Supabase Dashboard**
   - اذهب إلى: https://supabase.com/dashboard
   - اختر مشروعك

2. **افتح SQL Editor**
   - من القائمة الجانبية، اختر "SQL Editor"
   - انقر على "New query"

3. **نفذ السكريبت**
   - انسخ محتوى ملف `supabase_setup.sql` بالكامل
   - الصق الكود في محرر SQL
   - انقر على "Run" أو اضغط `Ctrl+Enter`

4. **تحقق من الجداول**
   - اذهب إلى "Table Editor" من القائمة الجانبية
   - يجب أن ترى الجداول التالية:
     - ✅ `users` - جدول المستخدمين
     - ✅ `patients` - جدول المرضى
     - ✅ `appointments` - جدول المواعيد
     - ✅ `visits` - جدول الزيارات والعلاجات

#### الطريقة الثانية: استخدام Supabase CLI (للمتقدمين)

```bash
# إذا لم تقم بتثبيت Supabase CLI
npm install -g supabase

# تسجيل الدخول
supabase login

# ربط المشروع
supabase link --project-ref dnzuljmguutvummtwljj

# تنفيذ السكريبت
supabase db push --file supabase_setup.sql
```

### 📝 الجداول المُنشأة

#### 1. جدول `users` (المستخدمين)
```sql
- id (UUID)
- email (TEXT)
- password_hash (TEXT)
- full_name (TEXT)
- role (admin/doctor/receptionist)
- created_at, updated_at
```

**حساب تجريبي تم إنشاؤه:**
- البريد: `admin@dental.com`
- كلمة المرور: `admin123`
- الدور: `admin`

#### 2. جدول `patients` (المرضى)
```sql
- id (UUID)
- name (TEXT)
- phone (TEXT)
- email (TEXT)
- date_of_birth (DATE)
- gender (TEXT)
- address (TEXT)
- medical_history (TEXT)
- allergies (TEXT)
- notes (TEXT)
- created_at, updated_at
```

#### 3. جدول `appointments` (المواعيد)
```sql
- id (UUID)
- patient_id (UUID) - مرتبط بجدول المرضى
- appointment_date (TIMESTAMP)
- duration_minutes (INTEGER)
- treatment_type (TEXT)
- status (مجدول/مكتمل/ملغي)
- notes (TEXT)
- reminder_sent (BOOLEAN)
- created_at, updated_at
```

#### 4. جدول `visits` (الزيارات)
```sql
- id (UUID)
- patient_id (UUID) - مرتبط بجدول المرضى
- visit_date (TIMESTAMP)
- diagnosis (TEXT)
- treatment (TEXT)
- prescription (TEXT)
- cost (DECIMAL)
- paid (BOOLEAN)
- notes (TEXT)
- created_at, updated_at
```

### 🔒 الأمان والصلاحيات

السكريبت يتضمن:
- ✅ Row Level Security (RLS) مفعّل على جميع الجداول
- ✅ Policies للسماح بالوصول للبيانات
- ✅ Indexes لتحسين الأداء
- ✅ Triggers لتحديث `updated_at` تلقائياً
- ✅ Foreign Keys للحفاظ على سلامة البيانات

### 🧪 اختبار الاتصال

بعد تنفيذ السكريبت، اختبر الاتصال:

```bash
cd dental-clinic-app
npm install
npm run dev
```

ثم:
1. افتح المتصفح على `http://localhost:5173`
2. سجل الدخول بالحساب التجريبي:
   - البريد: `admin@dental.com`
   - كلمة المرور: `admin123`

### 🌐 واجهة RTL (من اليمين لليسار)

التطبيق مُعد بالكامل للغة العربية مع:
- ✅ `dir="rtl"` في HTML
- ✅ Tailwind RTL configuration
- ✅ جميع النصوص بالعربية
- ✅ تصميم متجاوب يدعم RTL

### 📦 البيانات التجريبية

السكريبت يتضمن بيانات تجريبية:
- 3 مرضى
- 2 موعد (اليوم وغداً)
- حساب مسؤول واحد

### 🔍 التحقق من نجاح الإعداد

1. **في Supabase Dashboard:**
   - Table Editor → يجب أن ترى 4 جداول
   - Authentication → يمكنك إضافة مستخدمين من هنا لاحقاً

2. **في التطبيق:**
   - صفحة تسجيل الدخول تعمل
   - بعد الدخول، يمكنك رؤية المرضى والمواعيد
   - يمكنك إضافة مرضى جدد
   - يمكنك حجز مواعيد

### ⚠️ ملاحظات مهمة

1. **الأمان في الإنتاج:**
   - كلمات المرور حالياً غير مشفرة (للتطوير فقط)
   - في الإنتاج، استخدم Supabase Auth أو bcrypt لتشفير كلمات المرور

2. **الصلاحيات:**
   - حالياً RLS Policies تسمح بالوصول الكامل
   - في الإنتاج، قم بتقييد الصلاحيات حسب الدور

3. **النسخ الاحتياطي:**
   - قم بعمل نسخ احتياطي دوري من Supabase Dashboard
   - Settings → Database → Backups

### 🆘 حل المشاكل الشائعة

#### المشكلة: الجداول لم تُنشأ
**الحل:**
- تأكد من تنفيذ السكريبت بالكامل
- تحقق من وجود رسائل خطأ في SQL Editor
- تأكد من الاتصال بالإنترنت

#### المشكلة: خطأ في الاتصال من التطبيق
**الحل:**
```bash
# تأكد من تثبيت الحزم
npm install @supabase/supabase-js

# تأكد من ملف .env
cat .env  # في Linux/Mac
type .env  # في Windows

# أعد تشغيل الخادم
npm run dev
```

#### المشكلة: خطأ في تسجيل الدخول
**الحل:**
- تأكد من إدخال البريد وكلمة المرور الصحيحة
- تحقق من جدول `users` في Supabase
- تأكد من وجود المستخدم في قاعدة البيانات

### 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من console في المتصفح (F12)
2. تحقق من Logs في Supabase Dashboard
3. راجع ملف `README.md` للمزيد من التفاصيل

---

**تم الإعداد بواسطة:** Rovo Dev  
**التاريخ:** 2026-02-10  
**الإصدار:** 1.0.0
