# 🚀 ابدأ الآن - Get Started with Dental Clinic System

## 👋 مرحباً بك!

شكراً لاختيارك **نظام إدارة عيادة الأسنان**. هذا الدليل سيساعدك على البدء في أقل من 10 دقائق!

---

## ⏱️ خيارات البدء

### 🎯 الخيار 1: البدء السريع (موصى به للمبتدئين)

**الوقت المطلوب: 5-10 دقائق**

```bash
# 1. انتقل للمجلد
cd dental-clinic-app

# 2. شغّل سكريبت التثبيت
# Windows:
scripts\install.bat

# Mac/Linux:
chmod +x scripts/install.sh && ./scripts/install.sh

# 3. أعد ملف .env (راجع الخطوة التالية)

# 4. شغّل التطبيق
npm run dev
```

✅ **افتح المتصفح**: http://localhost:3000

---

### 🔧 الخيار 2: التثبيت اليدوي (للمطورين)

**الوقت المطلوب: 10-15 دقيقة**

#### الخطوة 1: التثبيت
```bash
cd dental-clinic-app
npm install
```

#### الخطوة 2: إعداد البيئة
```bash
cp .env.example .env
```

افتح `.env` وأضف:
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
```

#### الخطوة 3: إعداد قاعدة البيانات
1. اذهب إلى [supabase.com](https://supabase.com)
2. أنشئ مشروع جديد (مجاني)
3. اذهب إلى SQL Editor
4. انسخ والصق SQL من `SETUP_GUIDE.md`
5. نفذ الكود

#### الخطوة 4: التشغيل
```bash
npm run dev
```

---

## 🔑 بيانات الدخول الافتراضية

```
📧 البريد الإلكتروني: admin@dental.com
🔐 كلمة المرور: admin123
```

⚠️ **مهم**: غيّر كلمة المرور بعد أول تسجيل دخول!

---

## 📱 أول استخدام - جولة سريعة

### 1️⃣ استكشف لوحة التحكم
- شاهد الإحصائيات الفورية
- تصفح المواعيد القادمة
- جرّب الإجراءات السريعة

### 2️⃣ أضف أول مريض
```
القائمة الجانبية → سجل المرضى → إضافة مريض جديد
```
املأ البيانات:
- الاسم: محمد أحمد
- الهاتف: 0501234567
- العمر: 30
- الجنس: ذكر

### 3️⃣ احجز موعد
```
القائمة الجانبية → جدول المواعيد → حجز موعد جديد
```
اختر:
- المريض: محمد أحمد
- التاريخ: اليوم
- الوقت: 10:00 صباحاً
- المدة: 30 دقيقة

### 4️⃣ سجّل زيارة
```
القائمة الجانبية → سجل الزيارات → تسجيل زيارة جديدة
```
أدخل:
- المريض: محمد أحمد
- الخدمة: تنظيف
- التكلفة: 200 ريال
- الدفع: مدفوع

---

## 🎨 تخصيص النظام

### تغيير الألوان
افتح `tailwind.config.js`:
```javascript
colors: {
  primary: {
    500: '#0ea5e9', // غيّر هذا اللون
  }
}
```

### تغيير اسم العيادة
افتح `src/components/layout/Sidebar.jsx`:
```javascript
<h1>عيادة الأسنان</h1> // غيّر الاسم هنا
```

### إضافة شعار
ضع الشعار في `public/logo.png` ثم:
```javascript
<img src="/logo.png" alt="Logo" />
```

---

## 💬 إعداد الواتساب (اختياري)

### استخدام Evolution API

#### 1. تثبيت Evolution API
```bash
docker run -d \
  --name evolution-api \
  -p 8080:8080 \
  -e AUTHENTICATION_API_KEY=my-secret-key \
  atendai/evolution-api:latest
```

#### 2. إنشاء Instance
```bash
curl -X POST http://localhost:8080/instance/create \
  -H "Content-Type: application/json" \
  -H "apikey: my-secret-key" \
  -d '{"instanceName": "dental_clinic"}'
```

#### 3. مسح QR Code
افتح http://localhost:8080 وامسح QR Code

#### 4. إضافة المتغيرات
في `.env`:
```env
VITE_EVOLUTION_API_URL=http://localhost:8080
VITE_EVOLUTION_API_KEY=my-secret-key
VITE_EVOLUTION_INSTANCE_NAME=dental_clinic
```

#### 5. اختبار الإرسال
اذهب إلى موعد → اضغط "واتساب" → سيُرسل تلقائياً!

---

## 🔗 إعداد n8n (اختياري)

### للأتمتة المتقدمة

#### 1. تثبيت n8n
```bash
npm install -g n8n
```

#### 2. تشغيل n8n
```bash
n8n start
```

#### 3. استيراد Workflow
1. افتح http://localhost:5678
2. اذهب إلى Workflows → Import
3. استورد `n8n-workflows/appointment-reminder.json`
4. فعّل الـ workflow

#### 4. الحصول على Webhook URL
انسخ URL من الـ Webhook node

#### 5. إضافة في .env
```env
VITE_N8N_WEBHOOK_URL=your_webhook_url
```

---

## 🐛 المشاكل الشائعة وحلولها

### ❌ المشكلة: `npm install` يفشل
**الحل**:
```bash
# امسح المجلد وحاول مرة أخرى
rm -rf node_modules package-lock.json
npm install
```

### ❌ المشكلة: لا يمكن الاتصال بـ Supabase
**الحل**:
1. تحقق من صحة URL و API Key
2. تأكد من إنشاء الجداول
3. تحقق من اتصال الإنترنت

### ❌ المشكلة: الصفحة بيضاء
**الحل**:
```bash
# افتح Developer Console (F12)
# شاهد الأخطاء
# غالباً مشكلة في .env
```

### ❌ المشكلة: الواتساب لا يرسل
**الحل**:
1. تأكد من تشغيل Evolution API
2. تحقق من مسح QR Code
3. تأكد من صحة رقم الهاتف (05XXXXXXXX)

---

## 📚 الموارد المفيدة

### الوثائق
| الملف | متى تقرأه |
|------|-----------|
| `QUICK_START.md` | البدء السريع |
| `SETUP_GUIDE.md` | الإعداد التفصيلي |
| `FEATURES.md` | معرفة المميزات |
| `DEPLOYMENT.md` | عند النشر |

### الفيديوهات (قريباً)
- [ ] فيديو التثبيت
- [ ] فيديو الاستخدام
- [ ] فيديو التخصيص

---

## ✅ قائمة التحقق - Checklist

### قبل البدء
- [ ] تثبيت Node.js v18+
- [ ] إنشاء حساب Supabase
- [ ] تحميل المشروع

### الإعداد
- [ ] تثبيت المكتبات (`npm install`)
- [ ] إعداد ملف `.env`
- [ ] إنشاء جداول قاعدة البيانات
- [ ] اختبار التشغيل (`npm run dev`)

### أول استخدام
- [ ] تسجيل الدخول
- [ ] إضافة أول مريض
- [ ] حجز أول موعد
- [ ] تسجيل أول زيارة

### اختياري
- [ ] إعداد Evolution API
- [ ] إعداد n8n
- [ ] تخصيص الألوان والشعار

---

## 🎓 الخطوات التالية

### للمستخدمين
1. ✅ أضف جميع مرضاك الحاليين
2. ✅ احجز المواعيد القادمة
3. ✅ سجّل الزيارات اليومية
4. ✅ راقب الإحصائيات

### للمطورين
1. 🔧 خصص التصميم
2. 🔧 أضف ميزات جديدة
3. 🔧 ادمج مع أنظمة أخرى
4. 🔧 ساهم في المشروع (راجع `CONTRIBUTING.md`)

---

## 🆘 هل تحتاج مساعدة؟

### احصل على الدعم
- 📧 **البريد**: support@dentalclinic.com
- 💬 **GitHub Issues**: للمشاكل التقنية
- 📖 **الوثائق**: راجع ملفات `docs/`

### الأسئلة الشائعة
راجع `SETUP_GUIDE.md` قسم "استكشاف الأخطاء"

---

## 🎉 تهانينا!

أنت الآن جاهز لاستخدام نظام إدارة عيادة الأسنان!

### ماذا بعد؟
1. ✅ استكشف جميع المميزات
2. ✅ أضف بياناتك الحقيقية
3. ✅ خصص النظام حسب احتياجاتك
4. ✅ شارك تجربتك معنا

---

<div align="center">

**مستعد للبدء؟ شغّل الأمر التالي:**

```bash
npm run dev
```

**ثم افتح المتصفح على** http://localhost:3000

</div>

---

**بالتوفيق! 🚀**
